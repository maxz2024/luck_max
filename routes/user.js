// const { sendUserCreated, sendUser } = require("../controllers/user");
// const { creatUser, findUserByIdTelegram } = require("../middlewares/user");

// const usersRouter = require("express").Router();

// usersRouter.post("/users", creatUser, sendUserCreated)
// usersRouter.get("/users/:idTelegram", findUserByIdTelegram, sendUser)
// usersRouter.put("/users/:idTelegram", findUserByIdTelegram, editUser, sendUser)


// module.exports = { usersRouter}